import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headline: {
    fontSize: 24,
    fontWeight: '800',
    lineHeight: 29,
  },
  dark: {
    color: '#000',
  },
  primary: {
    color: '#27569C',
  },
});
