import React from 'react';
import { Text } from 'react-native';
import { styles } from './spinner.styles';

export const Spinner = () => {
  return <Text style={styles.spinner}>Загрузка...</Text>;
};
